{
  "name": "qvick-api",
  "version": "1.0.0",
  "description": "Azure Functions for Qvick",
  "main": "dist/functions/*.js",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "firebase-admin": "^13.6.0",
    "openai": "^4.77.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
